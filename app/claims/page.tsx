import { ComingSoon } from "@/components/ComingSoon";
export default function Page() {
  return <ComingSoon title="Claims" phase={2} desc="Log claims against a policy and track status from intimated to settled/rejected." />;
}
